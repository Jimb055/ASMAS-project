server:
  port: 8085

spring:
  application:
    name: response-collector
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    redis:
      enabled: false